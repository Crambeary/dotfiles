# Exposes the Electron main process's V8 inspector on localhost:19223 so
# ~/.local/bin/whispr-toggle can drive dictation start/stop, since COSMIC
# doesn't yet support the XDG GlobalShortcuts portal OpenWhispr needs for a
# real global hotkey (Electron's globalShortcut only fires while focused).
--inspect=19223
